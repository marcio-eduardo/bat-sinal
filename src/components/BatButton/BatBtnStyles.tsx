import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    
    fontSize: 26,
    fontWeight: 'bold',
    padding: 15,
    backgroundColor: '#333',
    color: '#fff',
    borderRadius: 5
  }
});